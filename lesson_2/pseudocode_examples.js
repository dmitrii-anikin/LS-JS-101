// a function that returns the sum of two numbers
/*
FUNCTION sum(num1, num2) {
  let result = num1 + num2
  return result
}
let total = sum(5,3)
*/

// a function that takes an array of strings, and returns a string that is all those strings concatenated together
/*
FUNCTION concatenateStrings(array):
    SET result = ""
    FOR EACH arrayItem:
        result = result + arrayItem
    RETURN result

// Example usage
SET concatenatedString = concatenateStrings(["str1", "str2", "str3"])
*/

// a method that takes an array of integers, and returns a new array with every other element from the original array, starting with the first element. For instance: everyOther([1,4,7,2,5]); // => [1,7,5]
